{
  "title": "Cala",
  "config": {
    "abort": {
      "mqtt_not_configured": "Home Assistant messaging (MQTT) is not set up. Please install and enable the MQTT integration, then try again.",
      "use_zeroconf": "Cala devices advertise themselves on the network. When one is discovered it will appear in the list above. Ensure the device is powered on and on the same network.",
      "invalid_discovery": "Invalid discovery information. Please try again.",
      "reauth_successful": "Reconnected successfully. The Cala integration has been reloaded."
    },
    "error": {
      "cannot_connect": "Failed to connect. Please try again.",
      "provisioning_failed": "Provisioning failed. Please try again.",
      "mqtt_not_configured": "Home Assistant messaging (MQTT) is not set up. Please install and enable the MQTT integration, then try again.",
      "invalid_host": "A valid device host or IP address is required.",
      "invalid_provisioning_code": "Provisioning code is required.",
      "invalid_mqtt_username": "MQTT username is required."
    },
    "step": {
      "user": {
        "menu_options": {
          "discovery": "Discovery (recommended)",
          "manual": "Manual setup"
        }
      },
      "manual": {
        "title": "Manual setup",
        "description": "Enter the Cala device network details.",
        "data": {
          "device_host": "Device host or IP address",
          "device_port": "Device port"
        }
      },
      "pair": {
        "data": {
          "pairing_code": "Pairing code"
        },
        "description": "Enter the pairing code shown on the Cala device (device_id: {device_id})."
      },
      "provision": {
        "description": "To pair your Cala you need to get the pairing code from the device: Settings → Advanced → Home Assistant.\nPlease create a unique MQTT username and password in your MQTT broker. Enter those credentials here.",
        "data": {
          "provisioning_code": "Provisioning code",
          "mqtt_username": "MQTT username",
          "mqtt_password": "MQTT password"
        },
        "sections": {
          "advanced": {
            "name": "Advanced",
            "description": "Broker host and port. Most users can leave these as default.",
            "data": {
              "mqtt_broker": "MQTT broker",
              "mqtt_port": "MQTT port"
            }
          }
        }
      },
      "init": {
        "menu_options": {
          "entities": "Entity mappings (solar, battery)",
          "reprovision": "Re-provision device (pairing code, broker, credentials)"
        }
      },
      "reprovision": {
        "title": "Re-provision device",
        "description": "Update the pairing code, MQTT broker, or credentials. Enter the device IP if needed (find it in your router or device display).",
        "data": {
          "device_host": "Device host or IP",
          "device_port": "Device port",
          "provisioning_code": "Provisioning code",
          "mqtt_username": "MQTT username",
          "mqtt_password": "MQTT password"
        }
      },
      "entities": {
        "title": "Entity mappings"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Cala",
        "description": "Choose what to configure.",
        "data": {
          "next_step": "What would you like to do?"
        }
      },
      "reprovision": {
        "title": "Re-provision device",
        "description": "Update the pairing code, MQTT broker, or credentials. Enter the device IP if needed (find it in your router or device display).",
        "data": {
          "device_host": "Device host or IP",
          "device_port": "Device port",
          "provisioning_code": "Provisioning code",
          "mqtt_username": "MQTT username",
          "mqtt_password": "MQTT password"
        }
      },
      "entities": {
        "title": "Entity mappings"
      }
    }
  }
}
